// Hero Banner Challenge – Mobile First
// Your Task:
// Create a responsive homepage with a hero banner that adapts to different screen sizes using media queries.

// Requirements
// A full-width banner image at the top of the page
// A large heading over or below the image
// A short subheading or tagline
// A call-to-action button (e.g. “Learn More” or “Get Started”)

// On Small Screens (Default / Mobile)
// Banner image takes full width of the screen
// Heading and button are stacked vertically and centered
// Font sizes are smaller and padding is minimal

// On Larger Screens (Using Media Queries)
// Heading and button can appear beside or overlaid on the banner image
// Font sizes and padding increase
// Button may shift to the right or below the heading

// Submission Checklist
// Page is built mobile-first (no media query needed for small screens)
// Uses at least one @media (min-width: …) query
// Text and button are readable and usable on small screens
// Layout adjusts nicely for tablets and desktops
// Code is well-organized (HTML and CSS are clean)

// Optional Ideas
// Try using flexbox to center content
// Add a hover effect to the button on desktop
// Use a placeholder image from:
// https://placehold.co/1200x400?text=Hero+Banner